# Generated by tests/scripts/gen-qpdf-fixtures.ts (needs the qpdf CLI). CI
# regenerates these each run; keep them out of version control so they can't
# drift across qpdf versions. LICENSE.qpdf (provenance) stays tracked.
*.pdf
